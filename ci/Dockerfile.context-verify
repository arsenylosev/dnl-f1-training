# Fast CI guard: fails if .dockerignore excludes these paths from the build context.
# Full `Dockerfile` uses COPY . /workspace — same context rules apply on GitHub and Cloud Build.
# Build: docker build -f ci/Dockerfile.context-verify .
#
# syntax=docker/dockerfile:1
FROM scratch
COPY models/foundation1_3s/model_config_3s.json /model_config_3s.json
COPY stable_audio_tools/configs/dataset_configs/gcs_f1_3s_pre_encode.json /gcs_f1_3s_pre_encode.json
COPY stable_audio_tools/configs/dataset_configs/pre_encoded_f1_3s.json /pre_encoded_f1_3s.json
